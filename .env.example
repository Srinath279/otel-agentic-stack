# Copy to .env and edit. The Makefile auto-loads it.

# mock = no API key, deterministic; claude = real Anthropic calls
LLM_MODE=mock

# Required only when LLM_MODE=claude
ANTHROPIC_API_KEY=

# Model used in claude mode
CLAUDE_MODEL=claude-opus-4-8
